@model IEnumerable<ResatSahinProje2021.Models.GraduateArchives>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/ManagamentPanel/Views/Shared/_Layout.cshtml";
}

<h2>Mezunlar</h2>

<p>
    @Html.ActionLink("Yeni Kayıt Ekle", "Create")
</p>
<table class="table">
    <tr>
        <th>
    Mezun Olduğu Bölüm
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
           @Html.DisplayFor(modelItem=>item.YearsName)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { id=item.ArchiveId }) |
            @Html.ActionLink("Detaylar", "Details", new { id=item.ArchiveId }) |
            @Html.ActionLink("Sil", "Delete", new { id=item.ArchiveId })
        </td>
    </tr>
}

</table>
